# .aipe/specs/

Optional user-authored spec/rule files that the aipe generators can reference
(e.g., coding standards, domain rules, stack notes). Empty for now — the
generators fall back to `.aipe/project/context.md` and the real repo.
